const s = "Promatch";

const up = s.substring(0, 3);

console.log(up);

const spacer = " hello ";
console.log(spacer.trim());

var nestedStr = 'Welcome To "Javascript" Tutorial';

console.log(nestedStr);
console.log(nestedStr.length);
console.log(nestedStr.toUpperCase());
console.log(nestedStr.toLowerCase());
console.log(nestedStr.startsWith("wel"));
console.log(nestedStr.replace("o", "ss"));
console.log(nestedStr.replaceAll("o", "ss"));
console.log(nestedStr);
